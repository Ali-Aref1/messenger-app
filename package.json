{
  "scripts":{
    "startall": "concurrently \"cd frontend && npm run dev -- --host\" \"cd backend && nodemon --watch server --ext ts,tsx --exec tsx server.ts\"",
    "update": "concurrently \"cd frontend && npm install\" \"cd backend && npm install\" \"npm install\""
  },
  "dependencies": {
    "concurrently": "^9.0.1",
    "nodemon": "^3.1.4"
  }
}
